export default function RotinaDashboard() {
  const habits = [
    "Não pegar o celular ao acordar",
    "Treinar sem mexer no celular",
    "Completar 2h de estudo",
    "Instagram máximo 2h",
    "Meditação + Buteyko",
    "Acompanhar futebol sem exagero",
  ];

  const studies = [
    ["SEG", "Governança", "ITIL 4 + COBIT"],
    ["TER", "Banco de Dados", "SQL + NoSQL"],
    ["QUA", "Engenharia de Software", "Scrum + UML"],
    ["QUI", "Segurança", "LGPD + ISO 27001"],
    ["SEX", "Redes + AWS", "OSI + EC2/S3/VPC"],
    ["SÁB", "Programação", "Python + Java"],
    ["DOM", "Simulado", "30–50 questões"],
  ];

  return (
    <div style={{
      minHeight: "100vh",
      background: "#f7f7f7",
      padding: "30px",
      fontFamily: "Arial"
    }}>
      <div style={{ maxWidth: "1100px", margin: "0 auto" }}>
        <h1 style={{ fontSize: "36px", marginBottom: "10px" }}>
          Dashboard de Rotina
        </h1>
        <p style={{ marginBottom: "30px" }}>
          Foco, disciplina, shape e menos procrastinação.
        </p>

        <div style={{ display: "grid", gap: "20px", gridTemplateColumns: "repeat(auto-fit, minmax(280px, 1fr))" }}>
          <div style={{ background: "white", padding: "20px", borderRadius: "16px" }}>
            <h2>Rotina Base</h2>
            <p><strong>08:00 → 17:00</strong> Trabalho</p>
            <p><strong>Pós-trabalho:</strong> Treino</p>
            <p><strong>Meta:</strong> 2h de estudo</p>
            <p><strong>Noite:</strong> Futebol + Relaxamento</p>
          </div>

          <div style={{ background: "white", padding: "20px", borderRadius: "16px" }}>
            <h2>Instagram</h2>
            <p>Atual: 4–5h/dia</p>
            <p><strong>Meta: máximo 2h/dia</strong></p>
            <p>Sem uso ao acordar, estudo e treino.</p>
          </div>

          <div style={{ background: "white", padding: "20px", borderRadius: "16px" }}>
            <h2>Regra Principal</h2>
            <p style={{ fontSize: "20px", fontWeight: "bold" }}>
              Disciplina {">"} motivação
            </p>
            <p>Executar mesmo sem vontade.</p>
          </div>
        </div>

        <div style={{ background: "white", padding: "20px", borderRadius: "16px", marginTop: "25px" }}>
          <h2>Checklist Diário</h2>
          <ul>
            {habits.map((item, i) => (
              <li key={i} style={{ marginBottom: "10px" }}>{item}</li>
            ))}
          </ul>
        </div>

        <div style={{ background: "white", padding: "20px", borderRadius: "16px", marginTop: "25px" }}>
          <h2>Plano Semanal de Estudos</h2>
          {studies.map(([day, subject, focus], i) => (
            <div key={i} style={{ padding: "12px 0", borderBottom: "1px solid #eee" }}>
              <strong>{day} — {subject}</strong>
              <p>{focus}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
